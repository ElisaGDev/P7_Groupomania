// Permet de transmettre des données sans avoir à le faire manuellement et à chaque fois
import { createContext } from "react";

// Uid = identifiant unique
export const UidContext = createContext();
